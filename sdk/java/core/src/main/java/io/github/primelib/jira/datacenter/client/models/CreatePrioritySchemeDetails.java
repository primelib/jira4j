// WARNING: This file was generated by PrimeCodeGen. DO NOT EDIT.

package io.github.primelib.jira.datacenter.client.models;

import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonPropertyOrder;
import com.fasterxml.jackson.annotation.JsonTypeName;
import java.util.List;
import java.util.Objects;
import java.util.function.Consumer;
import javax.annotation.processing.Generated;
import org.jetbrains.annotations.ApiStatus;

/**
 * CreatePrioritySchemeDetails
 * <p>
 * Details of a new priority scheme
 *
 */
@JsonTypeName("CreatePrioritySchemeDetails")
@JsonPropertyOrder({
    "defaultPriorityId",
    "description",
    "mappings",
    "name",
    "priorityIds",
    "projectIds"
})
@Generated(value = "io.github.primelib.primecodegen")
public class CreatePrioritySchemeDetails {

    /**
     * The ID of the default priority for the priority scheme.
     */
    @JsonProperty("defaultPriorityId")
    protected Long defaultPriorityId;

    /**
     * The description of the priority scheme.
     */
    @JsonProperty("description")
    protected String description;

    /**
     * Instructions to migrate the priorities of issues.

`in` mappings are used to migrate the priorities of issues to priorities used within the priority scheme.

`out` mappings are used to migrate the priorities of issues to priorities not used within the priority scheme.

 *  When **priorities** are **added** to the new priority scheme, no mapping needs to be provided as the new priorities are not used by any issues.
 *  When **priorities** are **removed** from the new priority scheme, no mapping needs to be provided as the removed priorities are not used by any issues.
 *  When **projects** are **added** to the priority scheme, the priorities of issues in those projects might need to be migrated to new priorities used by the priority scheme. This can occur when the current scheme does not use all the priorities in the project(s)' priority scheme(s).
    
     *  An `in` mapping must be provided for each of these priorities.
 *  When **projects** are **removed** from the priority scheme, no mapping needs to be provided as the removed projects are not using the priorities of the new priority scheme.

For more information on `in` and `out` mappings, see the child properties documentation for the `PriorityMapping` object below.
     */
    @JsonProperty("mappings")
    protected Object mappings;

    /**
     * The name of the priority scheme. Must be unique.
     */
    @JsonProperty("name")
    protected String name;

    /**
     * The IDs of priorities in the scheme.
     */
    @JsonProperty("priorityIds")
    protected List<Long> priorityIds;

    /**
     * The IDs of projects that will use the priority scheme.
     */
    @JsonProperty("projectIds")
    protected List<Long> projectIds;

    /**
     * Constructs a validated instance of {@link CreatePrioritySchemeDetails}.
     *
     * @param spec the specification to process
     */
    public CreatePrioritySchemeDetails(Consumer<CreatePrioritySchemeDetails> spec) {
        super();
        spec.accept(this);
    }

    /**
     * Protected no-args constructor for use by serialization frameworks.
     */
    @ApiStatus.Internal
    protected CreatePrioritySchemeDetails() {
    }
    /**
     * Constructs a validated instance of {@link CreatePrioritySchemeDetails}.
     * <p>
     * NOTE: This constructor is not considered stable and may change if the model is updated. Consider using {@link #CreatePrioritySchemeDetails(Consumer)} instead.
     * @param defaultPriorityId The ID of the default priority for the priority scheme.
     * @param description The description of the priority scheme.
     * @param mappings Instructions to migrate the priorities of issues.

`in` mappings are used to migrate the priorities of issues to priorities used within the priority scheme.

`out` mappings are used to migrate the priorities of issues to priorities not used within the priority scheme.

 *  When **priorities** are **added** to the new priority scheme, no mapping needs to be provided as the new priorities are not used by any issues.
 *  When **priorities** are **removed** from the new priority scheme, no mapping needs to be provided as the removed priorities are not used by any issues.
 *  When **projects** are **added** to the priority scheme, the priorities of issues in those projects might need to be migrated to new priorities used by the priority scheme. This can occur when the current scheme does not use all the priorities in the project(s)' priority scheme(s).
    
     *  An `in` mapping must be provided for each of these priorities.
 *  When **projects** are **removed** from the priority scheme, no mapping needs to be provided as the removed projects are not using the priorities of the new priority scheme.

For more information on `in` and `out` mappings, see the child properties documentation for the `PriorityMapping` object below.
     * @param name The name of the priority scheme. Must be unique.
     * @param priorityIds The IDs of priorities in the scheme.
     * @param projectIds The IDs of projects that will use the priority scheme.
     */
    @ApiStatus.Internal
    public CreatePrioritySchemeDetails(Long defaultPriorityId, String description, Object mappings, String name, List<Long> priorityIds, List<Long> projectIds) {
        this.defaultPriorityId = defaultPriorityId;
        this.description = description;
        this.mappings = mappings;
        this.name = name;
        this.priorityIds = priorityIds;
        this.projectIds = projectIds;
    }


    /**
     * Fluent getter for defaultPriorityId.
     * <p>
     * The ID of the default priority for the priority scheme.
     *
     * @return defaultPriorityId
     */
    public Long defaultPriorityId() {
        return this.defaultPriorityId;
    }

    /**
     * Fluent setter for defaultPriorityId.
     * <p>
     * The ID of the default priority for the priority scheme.
     *
     * @param defaultPriorityId defaultPriorityId
     * @return this
     */
    public CreatePrioritySchemeDetails defaultPriorityId(Long defaultPriorityId) {
        this.defaultPriorityId = defaultPriorityId;
        return this;
    }

    /**
     * Gets the value of defaultPriorityId.
     * <p>
     * The ID of the default priority for the priority scheme.
     *
     * @return defaultPriorityId
     */
    @JsonProperty("defaultPriorityId")
    public Long getDefaultPriorityId() {
        return this.defaultPriorityId;
    }

    /**
     * Sets the value of defaultPriorityId.
     * <p>
     * The ID of the default priority for the priority scheme.
     *
     * @param defaultPriorityId defaultPriorityId
     */
    public void setDefaultPriorityId(Long defaultPriorityId) {
        this.defaultPriorityId = defaultPriorityId;
    }
    /**
     * Fluent getter for description.
     * <p>
     * The description of the priority scheme.
     *
     * @return description
     */
    public String description() {
        return this.description;
    }

    /**
     * Fluent setter for description.
     * <p>
     * The description of the priority scheme.
     *
     * @param description description
     * @return this
     */
    public CreatePrioritySchemeDetails description(String description) {
        this.description = description;
        return this;
    }

    /**
     * Gets the value of description.
     * <p>
     * The description of the priority scheme.
     *
     * @return description
     */
    @JsonProperty("description")
    public String getDescription() {
        return this.description;
    }

    /**
     * Sets the value of description.
     * <p>
     * The description of the priority scheme.
     *
     * @param description description
     */
    public void setDescription(String description) {
        this.description = description;
    }
    /**
     * Fluent getter for mappings.
     * <p>
     * Instructions to migrate the priorities of issues.
     * 
     * `in` mappings are used to migrate the priorities of issues to priorities used within the priority scheme.
     * 
     * `out` mappings are used to migrate the priorities of issues to priorities not used within the priority scheme.
     * 
     *  *  When **priorities** are **added** to the new priority scheme, no mapping needs to be provided as the new priorities are not used by any issues.
     *  *  When **priorities** are **removed** from the new priority scheme, no mapping needs to be provided as the removed priorities are not used by any issues.
     *  *  When **projects** are **added** to the priority scheme, the priorities of issues in those projects might need to be migrated to new priorities used by the priority scheme. This can occur when the current scheme does not use all the priorities in the project(s)' priority scheme(s).
     *     
     *      *  An `in` mapping must be provided for each of these priorities.
     *  *  When **projects** are **removed** from the priority scheme, no mapping needs to be provided as the removed projects are not using the priorities of the new priority scheme.
     * 
     * For more information on `in` and `out` mappings, see the child properties documentation for the `PriorityMapping` object below.
     *
     * @return mappings
     */
    public Object mappings() {
        return this.mappings;
    }

    /**
     * Fluent setter for mappings.
     * <p>
     * Instructions to migrate the priorities of issues.
     * 
     * `in` mappings are used to migrate the priorities of issues to priorities used within the priority scheme.
     * 
     * `out` mappings are used to migrate the priorities of issues to priorities not used within the priority scheme.
     * 
     *  *  When **priorities** are **added** to the new priority scheme, no mapping needs to be provided as the new priorities are not used by any issues.
     *  *  When **priorities** are **removed** from the new priority scheme, no mapping needs to be provided as the removed priorities are not used by any issues.
     *  *  When **projects** are **added** to the priority scheme, the priorities of issues in those projects might need to be migrated to new priorities used by the priority scheme. This can occur when the current scheme does not use all the priorities in the project(s)' priority scheme(s).
     *     
     *      *  An `in` mapping must be provided for each of these priorities.
     *  *  When **projects** are **removed** from the priority scheme, no mapping needs to be provided as the removed projects are not using the priorities of the new priority scheme.
     * 
     * For more information on `in` and `out` mappings, see the child properties documentation for the `PriorityMapping` object below.
     *
     * @param mappings mappings
     * @return this
     */
    public CreatePrioritySchemeDetails mappings(Object mappings) {
        this.mappings = mappings;
        return this;
    }

    /**
     * Gets the value of mappings.
     * <p>
     * Instructions to migrate the priorities of issues.
     * 
     * `in` mappings are used to migrate the priorities of issues to priorities used within the priority scheme.
     * 
     * `out` mappings are used to migrate the priorities of issues to priorities not used within the priority scheme.
     * 
     *  *  When **priorities** are **added** to the new priority scheme, no mapping needs to be provided as the new priorities are not used by any issues.
     *  *  When **priorities** are **removed** from the new priority scheme, no mapping needs to be provided as the removed priorities are not used by any issues.
     *  *  When **projects** are **added** to the priority scheme, the priorities of issues in those projects might need to be migrated to new priorities used by the priority scheme. This can occur when the current scheme does not use all the priorities in the project(s)' priority scheme(s).
     *     
     *      *  An `in` mapping must be provided for each of these priorities.
     *  *  When **projects** are **removed** from the priority scheme, no mapping needs to be provided as the removed projects are not using the priorities of the new priority scheme.
     * 
     * For more information on `in` and `out` mappings, see the child properties documentation for the `PriorityMapping` object below.
     *
     * @return mappings
     */
    @JsonProperty("mappings")
    public Object getMappings() {
        return this.mappings;
    }

    /**
     * Sets the value of mappings.
     * <p>
     * Instructions to migrate the priorities of issues.
     * 
     * `in` mappings are used to migrate the priorities of issues to priorities used within the priority scheme.
     * 
     * `out` mappings are used to migrate the priorities of issues to priorities not used within the priority scheme.
     * 
     *  *  When **priorities** are **added** to the new priority scheme, no mapping needs to be provided as the new priorities are not used by any issues.
     *  *  When **priorities** are **removed** from the new priority scheme, no mapping needs to be provided as the removed priorities are not used by any issues.
     *  *  When **projects** are **added** to the priority scheme, the priorities of issues in those projects might need to be migrated to new priorities used by the priority scheme. This can occur when the current scheme does not use all the priorities in the project(s)' priority scheme(s).
     *     
     *      *  An `in` mapping must be provided for each of these priorities.
     *  *  When **projects** are **removed** from the priority scheme, no mapping needs to be provided as the removed projects are not using the priorities of the new priority scheme.
     * 
     * For more information on `in` and `out` mappings, see the child properties documentation for the `PriorityMapping` object below.
     *
     * @param mappings mappings
     */
    public void setMappings(Object mappings) {
        this.mappings = mappings;
    }
    /**
     * Fluent getter for name.
     * <p>
     * The name of the priority scheme. Must be unique.
     *
     * @return name
     */
    public String name() {
        return this.name;
    }

    /**
     * Fluent setter for name.
     * <p>
     * The name of the priority scheme. Must be unique.
     *
     * @param name name
     * @return this
     */
    public CreatePrioritySchemeDetails name(String name) {
        this.name = name;
        return this;
    }

    /**
     * Gets the value of name.
     * <p>
     * The name of the priority scheme. Must be unique.
     *
     * @return name
     */
    @JsonProperty("name")
    public String getName() {
        return this.name;
    }

    /**
     * Sets the value of name.
     * <p>
     * The name of the priority scheme. Must be unique.
     *
     * @param name name
     */
    public void setName(String name) {
        this.name = name;
    }
    /**
     * Fluent getter for priorityIds.
     * <p>
     * The IDs of priorities in the scheme.
     *
     * @return priorityIds
     */
    public List<Long> priorityIds() {
        return this.priorityIds;
    }

    /**
     * Fluent setter for priorityIds.
     * <p>
     * The IDs of priorities in the scheme.
     *
     * @param priorityIds priorityIds
     * @return this
     */
    public CreatePrioritySchemeDetails priorityIds(List<Long> priorityIds) {
        this.priorityIds = priorityIds;
        return this;
    }

    /**
     * Gets the value of priorityIds.
     * <p>
     * The IDs of priorities in the scheme.
     *
     * @return priorityIds
     */
    @JsonProperty("priorityIds")
    public List<Long> getPriorityIds() {
        return this.priorityIds;
    }

    /**
     * Sets the value of priorityIds.
     * <p>
     * The IDs of priorities in the scheme.
     *
     * @param priorityIds priorityIds
     */
    public void setPriorityIds(List<Long> priorityIds) {
        this.priorityIds = priorityIds;
    }
    /**
     * Fluent getter for projectIds.
     * <p>
     * The IDs of projects that will use the priority scheme.
     *
     * @return projectIds
     */
    public List<Long> projectIds() {
        return this.projectIds;
    }

    /**
     * Fluent setter for projectIds.
     * <p>
     * The IDs of projects that will use the priority scheme.
     *
     * @param projectIds projectIds
     * @return this
     */
    public CreatePrioritySchemeDetails projectIds(List<Long> projectIds) {
        this.projectIds = projectIds;
        return this;
    }

    /**
     * Gets the value of projectIds.
     * <p>
     * The IDs of projects that will use the priority scheme.
     *
     * @return projectIds
     */
    @JsonProperty("projectIds")
    public List<Long> getProjectIds() {
        return this.projectIds;
    }

    /**
     * Sets the value of projectIds.
     * <p>
     * The IDs of projects that will use the priority scheme.
     *
     * @param projectIds projectIds
     */
    public void setProjectIds(List<Long> projectIds) {
        this.projectIds = projectIds;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) return true;
        if (o == null || getClass() != o.getClass()) return false;
        CreatePrioritySchemeDetails that = (CreatePrioritySchemeDetails) o;
        return
            Objects.equals(this.defaultPriorityId, that.defaultPriorityId) &&
            Objects.equals(this.description, that.description) &&
            Objects.equals(this.mappings, that.mappings) &&
            Objects.equals(this.name, that.name) &&
            Objects.equals(this.priorityIds, that.priorityIds) &&
            Objects.equals(this.projectIds, that.projectIds);
    }

    @Override
    public int hashCode() {
        return Objects.hash(
            this.defaultPriorityId, 
            this.description, 
            this.mappings, 
            this.name, 
            this.priorityIds, 
            this.projectIds
        );
    }

    @Override
    public String toString() {
        return "CreatePrioritySchemeDetails{"
            + "defaultPriorityId=" + defaultPriorityId + ", "
            + "description=" + description + ", "
            + "mappings=" + mappings + ", "
            + "name=" + name + ", "
            + "priorityIds=" + priorityIds + ", "
            + "projectIds=" + projectIds
            + "}";
    }
}
