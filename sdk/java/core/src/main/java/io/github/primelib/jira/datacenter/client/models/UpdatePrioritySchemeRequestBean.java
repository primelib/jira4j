// WARNING: This file was generated by PrimeCodeGen. DO NOT EDIT.

package io.github.primelib.jira.datacenter.client.models;

import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonPropertyOrder;
import com.fasterxml.jackson.annotation.JsonTypeName;
import java.util.Objects;
import java.util.function.Consumer;
import javax.annotation.processing.Generated;
import org.jetbrains.annotations.ApiStatus;

/**
 * UpdatePrioritySchemeRequestBean
 * <p>
 * Details of a priority scheme.
 *
 */
@JsonTypeName("UpdatePrioritySchemeRequestBean")
@JsonPropertyOrder({
    "defaultPriorityId",
    "description",
    "mappings",
    "name",
    "priorities",
    "projects"
})
@Generated(value = "io.github.primelib.primecodegen")
public class UpdatePrioritySchemeRequestBean {

    /**
     * The default priority of the scheme.
     */
    @JsonProperty("defaultPriorityId")
    protected Long defaultPriorityId;

    /**
     * The description of the priority scheme.
     */
    @JsonProperty("description")
    protected String description;

    /**
     * Instructions to migrate the priorities of issues.

`in` mappings are used to migrate the priorities of issues to priorities used within the priority scheme.

`out` mappings are used to migrate the priorities of issues to priorities not used within the priority scheme.

 *  When **priorities** are **added** to the priority scheme, no mapping needs to be provided as the new priorities are not used by any issues.
 *  When **priorities** are **removed** from the priority scheme, issues that are using those priorities must be migrated to new priorities used by the priority scheme.
    
     *  An `in` mapping must be provided for each of these priorities.
 *  When **projects** are **added** to the priority scheme, the priorities of issues in those projects might need to be migrated to new priorities used by the priority scheme. This can occur when the current scheme does not use all the priorities in the project(s)' priority scheme(s).
    
     *  An `in` mapping must be provided for each of these priorities.
 *  When **projects** are **removed** from the priority scheme, the priorities of issues in those projects might need to be migrated to new priorities within the **Default Priority Scheme** that are not used by the priority scheme. This can occur when the **Default Priority Scheme** does not use all the priorities within the current scheme.
    
     *  An `out` mapping must be provided for each of these priorities.

For more information on `in` and `out` mappings, see the child properties documentation for the `PriorityMapping` object below.
     */
    @JsonProperty("mappings")
    protected Object mappings;

    /**
     * The name of the priority scheme. Must be unique.
     */
    @JsonProperty("name")
    protected String name;

    /**
     * The priorities in the scheme.
     */
    @JsonProperty("priorities")
    protected Object priorities;

    /**
     * The projects in the scheme.
     */
    @JsonProperty("projects")
    protected Object projects;

    /**
     * Constructs a validated instance of {@link UpdatePrioritySchemeRequestBean}.
     *
     * @param spec the specification to process
     */
    public UpdatePrioritySchemeRequestBean(Consumer<UpdatePrioritySchemeRequestBean> spec) {
        super();
        spec.accept(this);
    }

    /**
     * Protected no-args constructor for use by serialization frameworks.
     */
    @ApiStatus.Internal
    protected UpdatePrioritySchemeRequestBean() {
    }
    /**
     * Constructs a validated instance of {@link UpdatePrioritySchemeRequestBean}.
     * <p>
     * NOTE: This constructor is not considered stable and may change if the model is updated. Consider using {@link #UpdatePrioritySchemeRequestBean(Consumer)} instead.
     * @param defaultPriorityId The default priority of the scheme.
     * @param description The description of the priority scheme.
     * @param mappings Instructions to migrate the priorities of issues.

`in` mappings are used to migrate the priorities of issues to priorities used within the priority scheme.

`out` mappings are used to migrate the priorities of issues to priorities not used within the priority scheme.

 *  When **priorities** are **added** to the priority scheme, no mapping needs to be provided as the new priorities are not used by any issues.
 *  When **priorities** are **removed** from the priority scheme, issues that are using those priorities must be migrated to new priorities used by the priority scheme.
    
     *  An `in` mapping must be provided for each of these priorities.
 *  When **projects** are **added** to the priority scheme, the priorities of issues in those projects might need to be migrated to new priorities used by the priority scheme. This can occur when the current scheme does not use all the priorities in the project(s)' priority scheme(s).
    
     *  An `in` mapping must be provided for each of these priorities.
 *  When **projects** are **removed** from the priority scheme, the priorities of issues in those projects might need to be migrated to new priorities within the **Default Priority Scheme** that are not used by the priority scheme. This can occur when the **Default Priority Scheme** does not use all the priorities within the current scheme.
    
     *  An `out` mapping must be provided for each of these priorities.

For more information on `in` and `out` mappings, see the child properties documentation for the `PriorityMapping` object below.
     * @param name The name of the priority scheme. Must be unique.
     * @param priorities The priorities in the scheme.
     * @param projects The projects in the scheme.
     */
    @ApiStatus.Internal
    public UpdatePrioritySchemeRequestBean(Long defaultPriorityId, String description, Object mappings, String name, Object priorities, Object projects) {
        this.defaultPriorityId = defaultPriorityId;
        this.description = description;
        this.mappings = mappings;
        this.name = name;
        this.priorities = priorities;
        this.projects = projects;
    }


    /**
     * Fluent getter for defaultPriorityId.
     * <p>
     * The default priority of the scheme.
     *
     * @return defaultPriorityId
     */
    public Long defaultPriorityId() {
        return this.defaultPriorityId;
    }

    /**
     * Fluent setter for defaultPriorityId.
     * <p>
     * The default priority of the scheme.
     *
     * @param defaultPriorityId defaultPriorityId
     * @return this
     */
    public UpdatePrioritySchemeRequestBean defaultPriorityId(Long defaultPriorityId) {
        this.defaultPriorityId = defaultPriorityId;
        return this;
    }

    /**
     * Gets the value of defaultPriorityId.
     * <p>
     * The default priority of the scheme.
     *
     * @return defaultPriorityId
     */
    @JsonProperty("defaultPriorityId")
    public Long getDefaultPriorityId() {
        return this.defaultPriorityId;
    }

    /**
     * Sets the value of defaultPriorityId.
     * <p>
     * The default priority of the scheme.
     *
     * @param defaultPriorityId defaultPriorityId
     */
    public void setDefaultPriorityId(Long defaultPriorityId) {
        this.defaultPriorityId = defaultPriorityId;
    }
    /**
     * Fluent getter for description.
     * <p>
     * The description of the priority scheme.
     *
     * @return description
     */
    public String description() {
        return this.description;
    }

    /**
     * Fluent setter for description.
     * <p>
     * The description of the priority scheme.
     *
     * @param description description
     * @return this
     */
    public UpdatePrioritySchemeRequestBean description(String description) {
        this.description = description;
        return this;
    }

    /**
     * Gets the value of description.
     * <p>
     * The description of the priority scheme.
     *
     * @return description
     */
    @JsonProperty("description")
    public String getDescription() {
        return this.description;
    }

    /**
     * Sets the value of description.
     * <p>
     * The description of the priority scheme.
     *
     * @param description description
     */
    public void setDescription(String description) {
        this.description = description;
    }
    /**
     * Fluent getter for mappings.
     * <p>
     * Instructions to migrate the priorities of issues.
     * 
     * `in` mappings are used to migrate the priorities of issues to priorities used within the priority scheme.
     * 
     * `out` mappings are used to migrate the priorities of issues to priorities not used within the priority scheme.
     * 
     *  *  When **priorities** are **added** to the priority scheme, no mapping needs to be provided as the new priorities are not used by any issues.
     *  *  When **priorities** are **removed** from the priority scheme, issues that are using those priorities must be migrated to new priorities used by the priority scheme.
     *     
     *      *  An `in` mapping must be provided for each of these priorities.
     *  *  When **projects** are **added** to the priority scheme, the priorities of issues in those projects might need to be migrated to new priorities used by the priority scheme. This can occur when the current scheme does not use all the priorities in the project(s)' priority scheme(s).
     *     
     *      *  An `in` mapping must be provided for each of these priorities.
     *  *  When **projects** are **removed** from the priority scheme, the priorities of issues in those projects might need to be migrated to new priorities within the **Default Priority Scheme** that are not used by the priority scheme. This can occur when the **Default Priority Scheme** does not use all the priorities within the current scheme.
     *     
     *      *  An `out` mapping must be provided for each of these priorities.
     * 
     * For more information on `in` and `out` mappings, see the child properties documentation for the `PriorityMapping` object below.
     *
     * @return mappings
     */
    public Object mappings() {
        return this.mappings;
    }

    /**
     * Fluent setter for mappings.
     * <p>
     * Instructions to migrate the priorities of issues.
     * 
     * `in` mappings are used to migrate the priorities of issues to priorities used within the priority scheme.
     * 
     * `out` mappings are used to migrate the priorities of issues to priorities not used within the priority scheme.
     * 
     *  *  When **priorities** are **added** to the priority scheme, no mapping needs to be provided as the new priorities are not used by any issues.
     *  *  When **priorities** are **removed** from the priority scheme, issues that are using those priorities must be migrated to new priorities used by the priority scheme.
     *     
     *      *  An `in` mapping must be provided for each of these priorities.
     *  *  When **projects** are **added** to the priority scheme, the priorities of issues in those projects might need to be migrated to new priorities used by the priority scheme. This can occur when the current scheme does not use all the priorities in the project(s)' priority scheme(s).
     *     
     *      *  An `in` mapping must be provided for each of these priorities.
     *  *  When **projects** are **removed** from the priority scheme, the priorities of issues in those projects might need to be migrated to new priorities within the **Default Priority Scheme** that are not used by the priority scheme. This can occur when the **Default Priority Scheme** does not use all the priorities within the current scheme.
     *     
     *      *  An `out` mapping must be provided for each of these priorities.
     * 
     * For more information on `in` and `out` mappings, see the child properties documentation for the `PriorityMapping` object below.
     *
     * @param mappings mappings
     * @return this
     */
    public UpdatePrioritySchemeRequestBean mappings(Object mappings) {
        this.mappings = mappings;
        return this;
    }

    /**
     * Gets the value of mappings.
     * <p>
     * Instructions to migrate the priorities of issues.
     * 
     * `in` mappings are used to migrate the priorities of issues to priorities used within the priority scheme.
     * 
     * `out` mappings are used to migrate the priorities of issues to priorities not used within the priority scheme.
     * 
     *  *  When **priorities** are **added** to the priority scheme, no mapping needs to be provided as the new priorities are not used by any issues.
     *  *  When **priorities** are **removed** from the priority scheme, issues that are using those priorities must be migrated to new priorities used by the priority scheme.
     *     
     *      *  An `in` mapping must be provided for each of these priorities.
     *  *  When **projects** are **added** to the priority scheme, the priorities of issues in those projects might need to be migrated to new priorities used by the priority scheme. This can occur when the current scheme does not use all the priorities in the project(s)' priority scheme(s).
     *     
     *      *  An `in` mapping must be provided for each of these priorities.
     *  *  When **projects** are **removed** from the priority scheme, the priorities of issues in those projects might need to be migrated to new priorities within the **Default Priority Scheme** that are not used by the priority scheme. This can occur when the **Default Priority Scheme** does not use all the priorities within the current scheme.
     *     
     *      *  An `out` mapping must be provided for each of these priorities.
     * 
     * For more information on `in` and `out` mappings, see the child properties documentation for the `PriorityMapping` object below.
     *
     * @return mappings
     */
    @JsonProperty("mappings")
    public Object getMappings() {
        return this.mappings;
    }

    /**
     * Sets the value of mappings.
     * <p>
     * Instructions to migrate the priorities of issues.
     * 
     * `in` mappings are used to migrate the priorities of issues to priorities used within the priority scheme.
     * 
     * `out` mappings are used to migrate the priorities of issues to priorities not used within the priority scheme.
     * 
     *  *  When **priorities** are **added** to the priority scheme, no mapping needs to be provided as the new priorities are not used by any issues.
     *  *  When **priorities** are **removed** from the priority scheme, issues that are using those priorities must be migrated to new priorities used by the priority scheme.
     *     
     *      *  An `in` mapping must be provided for each of these priorities.
     *  *  When **projects** are **added** to the priority scheme, the priorities of issues in those projects might need to be migrated to new priorities used by the priority scheme. This can occur when the current scheme does not use all the priorities in the project(s)' priority scheme(s).
     *     
     *      *  An `in` mapping must be provided for each of these priorities.
     *  *  When **projects** are **removed** from the priority scheme, the priorities of issues in those projects might need to be migrated to new priorities within the **Default Priority Scheme** that are not used by the priority scheme. This can occur when the **Default Priority Scheme** does not use all the priorities within the current scheme.
     *     
     *      *  An `out` mapping must be provided for each of these priorities.
     * 
     * For more information on `in` and `out` mappings, see the child properties documentation for the `PriorityMapping` object below.
     *
     * @param mappings mappings
     */
    public void setMappings(Object mappings) {
        this.mappings = mappings;
    }
    /**
     * Fluent getter for name.
     * <p>
     * The name of the priority scheme. Must be unique.
     *
     * @return name
     */
    public String name() {
        return this.name;
    }

    /**
     * Fluent setter for name.
     * <p>
     * The name of the priority scheme. Must be unique.
     *
     * @param name name
     * @return this
     */
    public UpdatePrioritySchemeRequestBean name(String name) {
        this.name = name;
        return this;
    }

    /**
     * Gets the value of name.
     * <p>
     * The name of the priority scheme. Must be unique.
     *
     * @return name
     */
    @JsonProperty("name")
    public String getName() {
        return this.name;
    }

    /**
     * Sets the value of name.
     * <p>
     * The name of the priority scheme. Must be unique.
     *
     * @param name name
     */
    public void setName(String name) {
        this.name = name;
    }
    /**
     * Fluent getter for priorities.
     * <p>
     * The priorities in the scheme.
     *
     * @return priorities
     */
    public Object priorities() {
        return this.priorities;
    }

    /**
     * Fluent setter for priorities.
     * <p>
     * The priorities in the scheme.
     *
     * @param priorities priorities
     * @return this
     */
    public UpdatePrioritySchemeRequestBean priorities(Object priorities) {
        this.priorities = priorities;
        return this;
    }

    /**
     * Gets the value of priorities.
     * <p>
     * The priorities in the scheme.
     *
     * @return priorities
     */
    @JsonProperty("priorities")
    public Object getPriorities() {
        return this.priorities;
    }

    /**
     * Sets the value of priorities.
     * <p>
     * The priorities in the scheme.
     *
     * @param priorities priorities
     */
    public void setPriorities(Object priorities) {
        this.priorities = priorities;
    }
    /**
     * Fluent getter for projects.
     * <p>
     * The projects in the scheme.
     *
     * @return projects
     */
    public Object projects() {
        return this.projects;
    }

    /**
     * Fluent setter for projects.
     * <p>
     * The projects in the scheme.
     *
     * @param projects projects
     * @return this
     */
    public UpdatePrioritySchemeRequestBean projects(Object projects) {
        this.projects = projects;
        return this;
    }

    /**
     * Gets the value of projects.
     * <p>
     * The projects in the scheme.
     *
     * @return projects
     */
    @JsonProperty("projects")
    public Object getProjects() {
        return this.projects;
    }

    /**
     * Sets the value of projects.
     * <p>
     * The projects in the scheme.
     *
     * @param projects projects
     */
    public void setProjects(Object projects) {
        this.projects = projects;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) return true;
        if (o == null || getClass() != o.getClass()) return false;
        UpdatePrioritySchemeRequestBean that = (UpdatePrioritySchemeRequestBean) o;
        return
            Objects.equals(this.defaultPriorityId, that.defaultPriorityId) &&
            Objects.equals(this.description, that.description) &&
            Objects.equals(this.mappings, that.mappings) &&
            Objects.equals(this.name, that.name) &&
            Objects.equals(this.priorities, that.priorities) &&
            Objects.equals(this.projects, that.projects);
    }

    @Override
    public int hashCode() {
        return Objects.hash(
            this.defaultPriorityId, 
            this.description, 
            this.mappings, 
            this.name, 
            this.priorities, 
            this.projects
        );
    }

    @Override
    public String toString() {
        return "UpdatePrioritySchemeRequestBean{"
            + "defaultPriorityId=" + defaultPriorityId + ", "
            + "description=" + description + ", "
            + "mappings=" + mappings + ", "
            + "name=" + name + ", "
            + "priorities=" + priorities + ", "
            + "projects=" + projects
            + "}";
    }
}
