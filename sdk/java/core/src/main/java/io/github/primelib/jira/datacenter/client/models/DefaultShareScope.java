// WARNING: This file was generated by PrimeCodeGen. DO NOT EDIT.

package io.github.primelib.jira.datacenter.client.models;

import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonPropertyOrder;
import com.fasterxml.jackson.annotation.JsonTypeName;
import java.util.Objects;
import java.util.function.Consumer;
import javax.annotation.processing.Generated;
import org.jetbrains.annotations.ApiStatus;

/**
 * DefaultShareScope
 * <p>
 * Details of the scope of the default sharing for new filters and dashboards.
 *
 */
@JsonTypeName("DefaultShareScope")
@JsonPropertyOrder({
    "scope"
})
@Generated(value = "io.github.primelib.primecodegen")
public class DefaultShareScope {

    /**
     * The scope of the default sharing for new filters and dashboards:

 *  `AUTHENTICATED` Shared with all logged-in users.
 *  `GLOBAL` Shared with all logged-in users. This shows as `AUTHENTICATED` in the response.
 *  `PRIVATE` Not shared with any users.
     */
    @JsonProperty("scope")
    protected String scope;

    /**
     * Constructs a validated instance of {@link DefaultShareScope}.
     *
     * @param spec the specification to process
     */
    public DefaultShareScope(Consumer<DefaultShareScope> spec) {
        super();
        spec.accept(this);
    }

    /**
     * Protected no-args constructor for use by serialization frameworks.
     */
    @ApiStatus.Internal
    protected DefaultShareScope() {
    }
    /**
     * Constructs a validated instance of {@link DefaultShareScope}.
     * <p>
     * NOTE: This constructor is not considered stable and may change if the model is updated. Consider using {@link #DefaultShareScope(Consumer)} instead.
     * @param scope The scope of the default sharing for new filters and dashboards:

 *  `AUTHENTICATED` Shared with all logged-in users.
 *  `GLOBAL` Shared with all logged-in users. This shows as `AUTHENTICATED` in the response.
 *  `PRIVATE` Not shared with any users.
     */
    @ApiStatus.Internal
    public DefaultShareScope(String scope) {
        this.scope = scope;
    }


    /**
     * Fluent getter for scope.
     * <p>
     * The scope of the default sharing for new filters and dashboards:
     * 
     *  *  `AUTHENTICATED` Shared with all logged-in users.
     *  *  `GLOBAL` Shared with all logged-in users. This shows as `AUTHENTICATED` in the response.
     *  *  `PRIVATE` Not shared with any users.
     *
     * @return scope
     */
    public String scope() {
        return this.scope;
    }

    /**
     * Fluent setter for scope.
     * <p>
     * The scope of the default sharing for new filters and dashboards:
     * 
     *  *  `AUTHENTICATED` Shared with all logged-in users.
     *  *  `GLOBAL` Shared with all logged-in users. This shows as `AUTHENTICATED` in the response.
     *  *  `PRIVATE` Not shared with any users.
     *
     * @param scope scope
     * @return this
     */
    public DefaultShareScope scope(String scope) {
        this.scope = scope;
        return this;
    }

    /**
     * Gets the value of scope.
     * <p>
     * The scope of the default sharing for new filters and dashboards:
     * 
     *  *  `AUTHENTICATED` Shared with all logged-in users.
     *  *  `GLOBAL` Shared with all logged-in users. This shows as `AUTHENTICATED` in the response.
     *  *  `PRIVATE` Not shared with any users.
     *
     * @return scope
     */
    @JsonProperty("scope")
    public String getScope() {
        return this.scope;
    }

    /**
     * Sets the value of scope.
     * <p>
     * The scope of the default sharing for new filters and dashboards:
     * 
     *  *  `AUTHENTICATED` Shared with all logged-in users.
     *  *  `GLOBAL` Shared with all logged-in users. This shows as `AUTHENTICATED` in the response.
     *  *  `PRIVATE` Not shared with any users.
     *
     * @param scope scope
     */
    public void setScope(String scope) {
        this.scope = scope;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) return true;
        if (o == null || getClass() != o.getClass()) return false;
        DefaultShareScope that = (DefaultShareScope) o;
        return
            Objects.equals(this.scope, that.scope);
    }

    @Override
    public int hashCode() {
        return Objects.hash(
            this.scope
        );
    }

    @Override
    public String toString() {
        return "DefaultShareScope{"
            + "scope=" + scope
            + "}";
    }
}
